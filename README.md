# ts-hello-world

Minimal TypeScript Hello World project.

Quick start (Windows PowerShell):

```powershell
npm install
npm run dev
```

To build and run the compiled JS:

```powershell
npm run build
npm start
```

Files:
- `src/index.ts` — exports `greet(name: string)` and prints a greeting when run directly.
